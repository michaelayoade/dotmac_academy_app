{
  "schema_version": "dimensional-composition.v2",
  "product": "dotmac_academy_app",
  "starter_catalogue_revision": "a9dc45ecd00d5a0163b6544278888220082e2e75",
  "records": [
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-accounting",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-accounting''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ac'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_accounting` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-ai-operations",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-ai-operations''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ao'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_ai_operations` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-analytics",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-analytics''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ay'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_analytics` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-app-sync",
      "classification": "stateless-protocol-adapter",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'stateless-protocol-adapter' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'stateless-protocol-adapter' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_app_sync` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-application-directory",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-application-directory''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ad'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_application_directory` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-approvals",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-approvals''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ap'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_approvals` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-assets",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-assets''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_as'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_assets` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-auth-oidc",
      "classification": "stateless-protocol-adapter",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'stateless-protocol-adapter' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'stateless-protocol-adapter' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_auth_oidc` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-banking",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-banking''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_bk'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_banking` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-billing",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-billing''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_bi'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_billing` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-brand-profiles",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-brand-profiles''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_bp'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_brand_profiles` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-campaigns",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-campaigns''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ca'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_campaigns` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-collections",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-collections''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_cl'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_collections` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-commercial-agreements",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-commercial-agreements''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_cg'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_commercial_agreements` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-compliance-reporting",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-compliance-reporting''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_cr'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_compliance_reporting` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-connector-flutterwave",
      "classification": "stateless-protocol-adapter",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'stateless-protocol-adapter' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'stateless-protocol-adapter' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_connector_flutterwave` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-connector-linkedin",
      "classification": "stateless-protocol-adapter",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'stateless-protocol-adapter' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'stateless-protocol-adapter' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_connector_linkedin` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-connector-meta-social",
      "classification": "stateless-protocol-adapter",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'stateless-protocol-adapter' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'stateless-protocol-adapter' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_connector_meta_social` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-connector-mono",
      "classification": "stateless-protocol-adapter",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'stateless-protocol-adapter' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'stateless-protocol-adapter' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_connector_mono` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-connector-nira",
      "classification": "stateless-protocol-adapter",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'stateless-protocol-adapter' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'stateless-protocol-adapter' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_connector_nira` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-connector-paystack",
      "classification": "stateless-protocol-adapter",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'stateless-protocol-adapter' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'stateless-protocol-adapter' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_connector_paystack` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-connector-remita",
      "classification": "stateless-protocol-adapter",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'stateless-protocol-adapter' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'stateless-protocol-adapter' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_connector_remita` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-connector-whatsapp",
      "classification": "stateless-protocol-adapter",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'stateless-protocol-adapter' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'stateless-protocol-adapter' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_connector_whatsapp` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-content",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-content''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ct'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_content` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-customers",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-customers''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_cu'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_customers` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-deployment-control",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-deployment-control''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_dc'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_deployment_control` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-deployment-foundation",
      "classification": "universal-facility",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'universal-facility' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'universal-facility' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_deployment_foundation` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-document-rendering",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-document-rendering''s import package is not among the resolved origins",
        "migration_lineage": "classification 'optional-module' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_document_rendering` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-documents",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-documents''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_do'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_documents` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-durable-timers",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-durable-timers''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_dt'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_durable_timers` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-entitlement-allocation",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-entitlement-allocation''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ea'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_entitlement_allocation` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-expenses",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-expenses''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ex'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_expenses` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-fiber-plant",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-fiber-plant''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_fp'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_fiber_plant` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-files",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-files''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_fi'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_files` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-finance",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-finance''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_fn'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_finance` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-forms",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-forms''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_fm'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_forms` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-fulfillment",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-fulfillment''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_fu'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_fulfillment` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-fx-policy",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-fx-policy''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_fx'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_fx_policy` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-imports",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-imports''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_im'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_imports` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-inbox",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-inbox''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ib'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_inbox` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-inbox-operations",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-inbox-operations''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_io'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_inbox_operations` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-integration",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-integration''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ig'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_integration` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-inventory",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-inventory''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_iv'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_inventory` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-ipam",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-ipam''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ip'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_ipam` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-kernel",
      "classification": "universal-facility",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "true",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'universal-facility' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'universal-facility' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_kernel` reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-licensing",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-licensing''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_li'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_licensing` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-media-observations",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-media-observations''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_mo'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_media_observations` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-network-access",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-network-access''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_nac'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_network_access` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-network-assurance",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-network-assurance''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_na'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_network_assurance` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-network-control",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-network-control''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_nc'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_network_control` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-network-inventory",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-network-inventory''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ni'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_network_inventory` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-network-observability",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-network-observability''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_no'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_network_observability` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-network-topology",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-network-topology''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_nt'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_network_topology` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-numbering",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-numbering''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_nu'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_numbering` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-operational-escalations",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-operational-escalations''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_oe'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_operational_escalations` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-party",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-party''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_pt'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_party` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-payables",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-payables''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_pa'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_payables` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-payments",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-payments''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_pm'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_payments` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-payroll",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-payroll''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_py'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_payroll` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-people",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-people''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_pe'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_people` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-platform-health",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-platform-health''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ph'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_platform_health` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-pon-access",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-pon-access''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_pn'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_pon_access` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-positioning",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-positioning''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_po'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_positioning` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-procurement",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-procurement''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_pc'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_procurement` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-projects",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-projects''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_pj'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_projects` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-publishing",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-publishing''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_pb'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_publishing` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-qualification",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-qualification''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_qu'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_qualification` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-records",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-records''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_re'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_records` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-referrals",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-referrals''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_rf'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_referrals` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-release-catalog",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-release-catalog''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_rl'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_release_catalog` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-remote-access",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-remote-access''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ra'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_remote_access` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-reseller-management",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-reseller-management''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_rm'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_reseller_management` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-response-obligations",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-response-obligations''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ro'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_response_obligations` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-runner-transport",
      "classification": "universal-facility",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'universal-facility' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'universal-facility' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_runner_transport` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-runner-transport-github-actions",
      "classification": "stateless-protocol-adapter",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'stateless-protocol-adapter' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'stateless-protocol-adapter' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_runner_transport_github_actions` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-sales",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-sales''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_sa'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_sales` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-service-access-policy",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-service-access-policy''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_sap'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_service_access_policy` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-service-catalog",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-service-catalog''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_sc'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_service_catalog` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-service-changes",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-service-changes''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_sch'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_service_changes` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-service-orders",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-service-orders''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_so'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_service_orders` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-services",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-services''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_se'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_services` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-sites",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-sites''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_si'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_sites` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-subscriptions",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-subscriptions''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_su'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_subscriptions` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-support-access",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-support-access''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_sup'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_support_access` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-surveys",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-surveys''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_sv'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_surveys` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-tax",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-tax''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_tx'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_tax` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-template-studio",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-template-studio''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ts'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_template_studio` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-ticketing",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-ticketing''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_tk'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_ticketing` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-ui",
      "classification": "presentation-foundation",
      "installation": "unknown",
      "module_registration": "not_applicable",
      "migration_lineage": "not_applicable",
      "runtime_consumption": "true",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "classification 'presentation-foundation' \u2014 module_registration does not apply",
        "migration_lineage": "classification 'presentation-foundation' \u2014 migration_lineage does not apply",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_ui` reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-usage",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-usage''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_us'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_usage` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-usage-rating",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-usage-rating''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_ur'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_usage_rating` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-web-analytics",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-web-analytics''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_wa'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_web_analytics` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-work-orders",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-work-orders''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_wo'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_work_orders` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-workflow-runtime",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-workflow-runtime''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_wr'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_workflow_runtime` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    },
    {
      "schema_version": "dimensional-composition.v2",
      "product": "dotmac_academy_app",
      "distribution": "dotmac-workforce",
      "classification": "optional-module",
      "installation": "unknown",
      "module_registration": "false",
      "migration_lineage": "false",
      "runtime_consumption": "false",
      "evidence": {
        "installation": "No authoritative checked-in production install recipe exists in this tree. Searched: Dockerfile* (recursive, none found); docker-compose.prod.yml (one service 'db', image: postgres:16, no build: key); ci.yml, engineering-standards.yml (the only two workflow files under .github/workflows/, enumerated structurally \u2014 ci.yml's 'test' job installs the CI runner's test environment, not a deployed artifact; engineering-standards.yml's two jobs (pin-preflight, standards) install nothing at all); deploy/*.service ExecStart lines (each execs a pre-built .venv/bin/python directly, no install command, no ExecStartPre/ExecStartPost hook); Makefile, Procfile, fly.toml, render.yaml, app.yaml, .ebextensions/, ansible/, helm/, charts/, k8s/, terraform/, infra/, *.sh (none found); README.md's only 'poetry install' line sits under '## Local development', not a production artifact. Named, dismissed counter-evidence: README.md: \"Poetry's version is pinned \u2014 in CI and on the production host alike\" \u2014 names a production host running Poetry; supplies no install command; .github/workflows/ci.yml: \"must stay in step with the academy production host, which runs the same version from /usr/local/bin\" \u2014 names a production host running Poetry; supplies no install command. Neither is a recipe (no install command, no group selection, no artifact); installation stays unknown.",
        "module_registration": "app/assembly.py ProductAssemblySpec(modules=...) tuple contains ['academy_feature'] only, resolved to its import origin (see _resolve_registered_distribution_origins): 'academy_feature' is locally defined in assembly.py (FeatureManifest(...)), never imported from any package, so it resolves to no origin \u2014 'dotmac-workforce''s import package is not among the resolved origins",
        "migration_lineage": "alembic/versions/*.py scanned for schema-qualifying token 'mod_wf'; no occurrence",
        "runtime_consumption": "AST import graph from declared production entry points app.main and app.cli (README uvicorn command + every deploy/*.service ExecStart, each -m target refused rather than skipped if unparseable): `dotmac_workforce` not reached. Backstop: the set of top-level dotmac_* imports across ALL of app/**/*.py (not only entry-point-reachable ones) is independently confirmed to be exactly {dotmac_kernel, dotmac_ui}."
      }
    }
  ]
}
